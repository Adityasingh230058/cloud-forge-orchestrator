[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cloud-forge-orchestrator"
version = "1.0.0"
description = "Automated Multi-Server Private Cloud & Kubernetes Cluster Orchestrator with MetalLB and Security Hardening"
readme = "README.md"
authors = [{ name = "Aditya Singh", email = "adityasingh120444@gmail.com" }]
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "pyyaml>=6.0",
    "jinja2>=3.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
]

[project.scripts]
cloud-forge = "cloud_forge.cli:app"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
